import React from "react";

const Logo = ({ w, h }) => {
  return (
    <>
    <div width={w} height={h} className="bg-red-600 text-white p-2 rounded-md">Raj's Shop</div>
    </>
  );
};

export default Logo;
